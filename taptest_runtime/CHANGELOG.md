## 1.0.0

First stable release.

* Requires Flutter 3.47. `RuntimeParams.themeMode` is `package:material_ui`'s `ThemeMode`, not the in-SDK Material type.

* `RuntimeParams` for theme mode, locale, initial route, and test extensions
