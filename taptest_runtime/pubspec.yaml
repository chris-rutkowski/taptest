name: taptest_runtime
description: Production-safe runtime for TapTest. Exposes RuntimeParams so tests can drive theme, locale, and routing.
version: 1.0.0
homepage: https://taptest.dev
repository: https://github.com/chris-rutkowski/taptest
issue_tracker: https://github.com/chris-rutkowski/taptest/issues
documentation: https://taptest.dev
topics:
  - testing
  - flutter

environment:
  sdk: ^3.11.0
  flutter: ">=3.35.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

flutter:
