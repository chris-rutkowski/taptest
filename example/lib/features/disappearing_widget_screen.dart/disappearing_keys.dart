import 'package:material_ui/material_ui.dart';

const _prefix = 'Disappearing_';

abstract class DisappearingKeys {
  static const screen = ValueKey('${_prefix}screen');
  static const disappearingWidget = ValueKey('${_prefix}disappearingWidget');
}
