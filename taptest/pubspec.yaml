name: taptest
description: "A new Flutter package project."
version: 0.0.1
homepage:
publish_to: none

environment:
  sdk: ^3.11.0
  flutter: ">=1.17.0"

dependencies:
  crypto:
  device_info_plus:
  fake_http_client:
  file:
  flutter:
    sdk: flutter
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  meta:
  path:
  platform:
  qr: ^4.0.0
  taptest_runtime:
    path: ../taptest_runtime

dev_dependencies:
  flutter_lints:
  go_router:

flutter:
